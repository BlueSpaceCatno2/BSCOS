# BSCOS
A (mostly) written in C real-time-operating-system for hardware testing @ debugging. Not quite finished, but ideal for situations like ones I end up in where linux is too bloated and the BIOS cannot be tuned for my issues.
